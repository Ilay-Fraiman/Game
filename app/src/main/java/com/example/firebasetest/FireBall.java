package com.example.firebasetest;

import android.graphics.Bitmap;

public class FireBall extends Projectile{
    public FireBall(Bitmap sprite, int ID, Character creator)
    {
        super(sprite, ID, creator);
    }
}
