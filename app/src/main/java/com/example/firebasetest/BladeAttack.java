package com.example.firebasetest;

import android.graphics.Bitmap;

public class BladeAttack extends Projectile{
    public BladeAttack(Bitmap sprite, int ID, Character creator)
    {
        super(sprite, ID, creator);
    }
}
