package com.example.firebasetest;

import android.graphics.Bitmap;

public class Mist extends Projectile{
    public Mist(Bitmap sprite, int ID, Character creator)
    {
        super(sprite, ID, creator);
    }
}
